@model IEnumerable<Grm.BaseDados.WebAppl.Models.sindicato.Pais>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodIbge)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiglaPais)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomePais)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodIbge)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiglaPais)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomePais)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPais }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPais }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPais })
        </td>
    </tr>
}

</table>
